 // subsonic credentials
 var SUBSONIC_URL = "http://localhost:4040";
 var SUBSONIC_API_URL = SUBSONIC_URL+"/rest"
 var SUBSONIC_API_CREDENTIALS = {
          u : 'admin',
          p : 'admin',
          c : 'myJukebox',
          v : '1.12.0',
          f : 'json'
      };

  // subsonic playlist name
  var SUBSONIC_PLAYSLIST_NAME = "myjukebox";
  
  // max thumbs by page
  var MAX_THUMBS=8;
  
  // img to display when no covert art available
  var NO_COVER_URL="img/nocover.png"
  
  // cover art size (ok for 1440x900, try 250 for 1920*1080)
  var COVER_SIZE=218;
  
  // time before modal popup disappear
  var MODAL_TIMEOUT=1500;


